# Smart-Attendance-System-using-Face-Recognition
The Smart Attendance System is an AI-driven solution that replaces manual roll calls using real-time face recognition. It uses a Caffe-based SSD model to detect faces with 99.99% accuracy. By converting features into 128-D encodings, it identifies students and logs their presence into a CSV file.
